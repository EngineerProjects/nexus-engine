{
  "mcpServers": {
    "docling": {
      "type": "stdio",
      "command": "uvx",
      "args": ["--from", "docling-mcp", "docling-mcp-server", "--transport", "stdio"],
      "env": {
        "DOCLING_MCP_SERVICE_URL": "http://localhost:5001",
        "DOCLING_MCP_CONVERSION_MODE": "remote"
      }
    }
  }
}
